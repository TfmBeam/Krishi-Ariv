name: krishi_mithra
description: A Flutter app for agricultural assistance and guidance.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image_picker: ^1.0.4
  
  permission_handler: ^12.0.1
  audioplayers: ^6.5.1
  path_provider: ^2.1.1
  http: ^1.1.0
  file_picker: ^10.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/backgrounds/
